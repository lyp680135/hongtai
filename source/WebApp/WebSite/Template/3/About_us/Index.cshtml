@page
@inject ICMSService _cmsServices;
@{
    ViewData["Tag"] = 1;
    ViewData["Title"] = "公司简介";
    ViewData["Desc"] = "公司简介";
    var mngSetting = _cmsServices.GetSetting();
}
@section head{
    <link href="/Content/css/intro.css" rel="stylesheet" />
}

<!--------------------------面包屑------------------------------>
<div class="location_box">
    <div class="location">
        <span>&nbsp;公司简介</span>
        <span>您当前的位置： 首页 > </span>
        <div class="clearfix"></div>
    </div>
</div>
<!--------------------------公司简介------------------------------>
<div class="introduce_box">
    <div class="introduce1">
        <img src="/Content/images/intro_pic.jpg" />
        <div class="company_intro">
            @{
                var Intro = _cmsServices.GetSinglePage("关于鸿泰1");
                {
                    if (Intro != null)
                    {
                        @Html.Raw(Util.Extensions.ToHtml(Intro.Content).ToString().Replace("<img src=\"/../", "<img src=\"" + mngSetting.Domain_PC))
                    }
                }
            }
        </div>
        <div class="clearfix"></div>
    </div>
</div>


<div class="introduce2">
    @{
        var Intro2 = _cmsServices.GetSinglePage("关于鸿泰2");
        {
            if (Intro2 != null)
            {
                @Html.Raw(Util.Extensions.ToHtml(Intro2.Content).ToString().Replace("<img src=\"/../", "<img src=\"" + mngSetting.Domain_PC))
            }
        }
    }
    <div class="clearfix"></div>
</div>

@{
    var Intro3 = _cmsServices.IcontentColumnDataService.GetContentColumnDataList("关于鸿泰3", 1, 100);
    if (Intro3 != null)
    {
        @for (int i = 0; i < Intro3.Count; i++)
        {
            <div class="intro_element">
                @if (i % 2 == 0)
                {
                    <div class="element_row element_row1">

                        <img src="@mngSetting.Domain_PC.Substring(0, mngSetting.Domain_PC.Length - 1)@Intro3[i]["Image"]" />
                        <div class="tabulation">
                            @Html.Raw(Util.Extensions.ToHtml(Intro3[i]["Content"].ToString()).ToString().Replace("<img src=\"/../", "<img src=\"" + mngSetting.Domain_PC))
                        </div>

                    </div>
                }
                else
                {
                    <div class="element_row element_row2">

                        <img src="@mngSetting.Domain_PC.Substring(0, mngSetting.Domain_PC.Length - 1)@Intro3[i]["Image"]" />
                        <div class="tabulation">
                            @Html.Raw(Util.Extensions.ToHtml(Intro3[i]["Content"].ToString()).ToString().Replace("<img src=\"/../", "<img src=\"" + mngSetting.Domain_PC))
                        </div>

                    </div>
                }
                <div class="clearfix"></div>
            </div>
        }
    }
}

<style>
    .introduce2 img {
        display: block;
        margin-left: auto;
        margin-right: auto;
        margin-bottom:20px;
    }
</style>
